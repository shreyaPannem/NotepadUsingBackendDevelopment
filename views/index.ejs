<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-900">
      <div class="form p-10 text-white">
        <form action="/create" method="post">
          <input
            type="text"
            placeholder="title goes here"
            class="block px-5 py-2 bg-zinc-800 rounded-md outline-none"
            name="title"
          />
          <textarea
            name="details"
            id=""
            placeholder="write your task description"
            class="resize-none mt-2 block px-5 py-3 bg-zinc-800 w-full rounded-md outline-none" 
          ></textarea>
          <input
            type="submit"
            value="create task"
            class="mt-2 bg-blue-600 px-5 py-2 rounded-md"
          />
        </form>
      </div>
      <div class="tasks flex gap-3 flex-wrap p-10">
        <% if(files.length > 0) { %> <% files.forEach((file)=>{ %>
        <div class="task w-72 px-3 py-4 rounded-md bg-zinc-800">
          <h1 class="text-white text-3xl tracking-tighter">
            <%=file%>
          </h1>
          <div class="flex w-full justify-between items-center mt-3">
          <a href="/file/<%=file%>" class="text-blue-500 inline-block">Read more.</a>
          <a href="/edit/<%=file%>" class="text-zinc-500"> edit</a>
        </div>
        </div>
        <%})%> <% } else { %>
        <h1 class="text-zinc-600 px-3">no tasks yet</h1>
        <% } %>
      </div>
    </div>
  </body>
</html>
